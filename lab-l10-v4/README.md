## SETUP

1. `npm install`
2. `cp weatherserver/.env.example weatherserver/.env`
3. Add your OpenWeather API key to weatherserver/.env
4. Terminal 1: `npm run lars start`
5. Terminal 2: `cd weatherserver && npm run start`